# LLM（智谱，OpenAI 兼容接口）
ZHIPU_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4
ZHIPU_MODEL=glm-4-flash

# Voyager 运行参数
VOYAGER_ROOT=/Users/zxydediannao/Downloads/Voyager-main
MC_PORT=25565
MAX_ITERATIONS=200

# 说明：
# - 世界就是 Voyager 连接的那个 MC 世界（默认 localhost:25565），
#   不需要单独的 Drift 服务器。
# - 把 bot 设为 op，否则 /setblock /summon /give 等世界补丁无法执行。
